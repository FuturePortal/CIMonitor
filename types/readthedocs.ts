export type ReadTheDocsBuild = {
	event: string;
	name: string;
	slug: string;
	version: string;
	commit: string;
	build: string;
	start_date: string;
	build_url: string;
	docs_url: string;
};

export default ReadTheDocsBuild;
