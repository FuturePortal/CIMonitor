import styled from 'styled-components';

export const WarningBar = styled.div`
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    font-size: 4rem;
    background: rgba(208, 0, 0, 0.8);
    color: white;
    padding: 1rem;
    text-align: center;
`;
