import styled from 'styled-components';

import { stateColor } from '/frontend/style/colors';

export const DeleteButton = styled.button`
    background: ${stateColor.warning};
    padding: 0.5rem;
    margin: 1rem 0;
`;
