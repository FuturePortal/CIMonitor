class ModuleManager {
    init() {
        console.log('[module/manager] Init.');

        // TODO: fetch triggers and events from the config

        // TODO: watch for status events if has events
    }

    // TODO: on event, check if a trigger matches the event

    // TODO: fire module for the matched event
}

export default new ModuleManager();
